# Skills own workflow and references own knowledge

The four `ddd-expert` skills own phase behavior, acceptance, routing, stopping, and output contracts; references own reusable DDD knowledge, House Style, verified API guidance, and lightweight leaf navigation. We will retire `ddd-agent-contract.md` instead of introducing a global interpretation layer. A small amount of phase-specific repetition is preferred to making every skill load an indirect workflow contract.
