The Order Model at `docs/ddd-expert/context/order/model.md` is confirmed and
model-ready. Implement its accepted `RenameOrder` behavior directly. The
existing test expresses the required behavior and must not be edited. Make the
smallest production-code change and run the focused Go tests.
