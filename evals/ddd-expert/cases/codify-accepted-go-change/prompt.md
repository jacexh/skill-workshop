Implement the accepted `RenameOrder` change from `docs/design.md`. The existing
test expresses the required behavior and must not be edited. Make the smallest
production-code change and run the focused Go tests.
