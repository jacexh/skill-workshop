Implement the accepted `RenameOrder` behavior from the confirmed Order Model
at `docs/ddd-expert/context/order/model.md`. The existing test expresses the
required behavior and must not be edited. Make the smallest production-code
change and run the focused Go tests.
