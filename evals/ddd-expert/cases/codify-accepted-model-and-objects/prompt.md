The current Order Model and Domain Objects are accepted. Implement Order's
`Rename` behavior exactly as recorded. The existing test expresses the required
result and must not be edited. Keep DDD artifacts read-only, realize the behavior
as an Order method, and run the focused Go tests.
