The existing DDD artifacts use the retired Context Map structure. Business
owners explicitly accept `docs/accepted-context-map-migration.md` as the
complete integrated Model target for this scope. Use Explore to replace the
legacy Context Map and update every affected Model once, preserving the source
decision and context inventory. Apply the accepted root README terminology
replacement in the same transaction. No business fact remains open and no Design is
authorized.
